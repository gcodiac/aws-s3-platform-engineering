# Cloud Launchpad

A static website for a cloud platform-engineering project: hand-written HTML, CSS
and vanilla JavaScript, with no framework and no build step.

## Run it locally

```bash
python3 -m http.server 8080
```

Then open <http://localhost:8080>.
